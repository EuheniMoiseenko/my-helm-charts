apiVersion: v1
kind: Service
metadata:
  name: {{ include "rabbitmg.fullname" . }}
  labels:
    {{- include "rabbitmg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rabbitmg.selectorLabels" . | nindent 4 }}
