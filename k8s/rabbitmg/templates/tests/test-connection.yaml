apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rabbitmg.fullname" . }}-test-connection"
  labels:
    {{- include "rabbitmg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rabbitmg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
