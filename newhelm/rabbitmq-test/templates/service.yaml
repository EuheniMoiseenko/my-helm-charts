apiVersion: v1
kind: Service
metadata:
  name: {{ include "rabbitmq-test.fullname" . }}
  labels:
    {{- include "rabbitmq-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rabbitmq-test.selectorLabels" . | nindent 4 }}
